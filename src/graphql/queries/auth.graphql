query GetCurrentUser {
  getCurrentUser {
    id
    email
    name
    imageUrl
    isActive
    isEmailConfirmed
    jwtToken
    createdAt
    updatedAt
    logo {
      id
      url
      avatarUrl
    }
  }
}
